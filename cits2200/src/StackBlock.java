import CITS2200.stack;
import CITS2200.underflow;
import CITS2200.overflow;

public class StackBlock implements Sort {

//  Create an empty stack of size size
    public void stackBlock(int size)
    {

    }

//  Checks to see if the stack is empty, otherwise returns false
    public boolean isEmpty()
    {
        return true;
    }

//  Checks to see if the stack is full, otherwise returns false
    public boolean isFull()
    {
        return true;
    }

//  Push Object o onto the top of the stack, or throw an overflow exception if the stack is full
    public void push(Object o)
    {

    }

//  Returns the object on the top of the stack, or throws an underflow exception if the stack is empty
    public Object examine()
    {
        return 0;
    }

//  Removes and returns the object on the top of the stack, or throws an underflow exception if the stack is empty
    public Object pop()
    {
        return 0;
    }
}